const h = client.innerHeight;
const w = client.innerWidth;
console.log(h, w);

export const element = `
<svg viewbox=(0 0 ${h} ${w})>
    <g fill="#ae0700">
        <circle class="circle" cx="420" cy="296" 
    </g>
</svg>
`